import { BoldMarkdownPipe } from './bold-markdown.pipe';

describe('BoldMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
